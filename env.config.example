# =============================================================================
# CRYPTO TRADING BOT - Environment Variables (New Config System)
# =============================================================================
# This file is for the new YAML-based configuration system
# Copy this to .env in the project root and fill in your values
# NEVER commit .env to version control!

# Environment (development, staging, production)
# This determines which YAML config file is loaded from config/environments/
ENVIRONMENT=development

# Database Credentials (REQUIRED)
DATABASE_USER=crypto_bot_user
DATABASE_PASSWORD=your_secure_password_here

# Redis Password (optional)
# REDIS_PASSWORD=your_redis_password

# Security - REQUIRED
# Generate strong keys: python -c "import secrets; print(secrets.token_urlsafe(32))"
ENCRYPTION_KEY=your_32_char_encryption_key_here_minimum
JWT_SECRET=your_jwt_secret_key_here

# Exchange API Keys (REQUIRED for trading)
BINANCE_API_KEY=your_binance_api_key_here
BINANCE_API_SECRET=your_binance_api_secret_here

COINBASE_API_KEY=your_coinbase_api_key_here
COINBASE_API_SECRET=your_coinbase_api_secret_here
COINBASE_PASSPHRASE=your_coinbase_passphrase_here

# Notifications (optional)
TELEGRAM_BOT_TOKEN=your_telegram_bot_token
TELEGRAM_CHAT_ID=your_telegram_chat_id

DISCORD_WEBHOOK_URL=your_discord_webhook_url

EMAIL_SMTP_PASSWORD=your_email_smtp_password

# =============================================================================
# NOTES:
# =============================================================================
# - Non-sensitive configuration (ports, hosts, timeouts) is in config/environments/*.yaml
# - Only put sensitive data (passwords, API keys, secrets) in .env
# - Use config/environments/development.yaml for development settings
# - Use config/environments/production.yaml for production settings
# - Run: cp env.config.example .env  # then edit .env with your values
# - See config/README.md for more details

